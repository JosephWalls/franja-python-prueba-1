#Escribir un programa que le pregunte al usuario una cantidad a invertir, 
#el interés porcentual anual y el número de años, y muestre por pantalla el capital obtenido en la 
#inversión redondeado a dos decimales.

caninv=int(input("ingrese el monto que quiere invertir: "))
poranual=float(input("ingrese el interes porcentual anual: "))
años=int(input("ingrese el numero de años que desea inverti: "))


cantidad_final=caninv*((poranual/100)+1)*años
cantidad_final=round(cantidad_final,2)
cantidad_final=str(cantidad_final)
print("el capital obtenido en la inversion es: "+cantidad_final)
